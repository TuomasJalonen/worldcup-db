# worldcup-db
Bash + PostgreSQL project for World Cup data. insert_data.sh imports teams and games from CSV into the database, queries.sh runs analytical queries, and worldcup.sql defines the schema. Demonstrates joins, aggregates, subqueries, and shell scripting with psql.
